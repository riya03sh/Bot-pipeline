[build]
builder = "NIXPACKS"

[deploy]
# Change: Remove 'backend.' from the start command
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5