[build]
builder = "NIXPACKS"

[deploy]
# This tells Railway: "From the root, go into the backend folder, 
# find the main module, and run the app."
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"